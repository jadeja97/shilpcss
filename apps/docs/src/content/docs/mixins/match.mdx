---
title: "@match Mixin - Selector Matching"
description: Use @match mixin for complex selector patterns in Shilp CSS conditional styling.
keywords:
  - "@match"
  - selector matching
  - pattern matching
  - custom selectors
authors:
  - jadeja
publishedAt: "2026-04-20T05:11:25Z"
lastModifiedAt: "2026-05-23T14:20:17Z"
---

# @match Mixin

Use `@match` mixin for complex selector patterns.

Usage:

```css
.any-class {
	/* functions */
	@match <variant>("<arguments>") { ... }
}
```

<Alert variant="info">

This page contains only the **mixins configuration reference**. Complete documentation and usage
examples will be added progressively in future updates.

</Alert>

---

<section>

## Reference

```js title="shilp.config.js"
/** @type {import('shilpcss/types').ShilpConfig} */
const shilpConfig = {
  source: "react",

  mixins: {
    match: {
      variants: {
        /* functions */
        not: "&:not(<1>)",
        is: "&:is(<1>)",
        where: "&:where(<1>)",
      },
    },
  },
};

export default shilpConfig;
```

</section>
