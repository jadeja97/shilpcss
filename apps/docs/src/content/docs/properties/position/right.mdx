---
title: "Right Offset | @position"
description: "Set positioned element's right side distance or offset from the parent."
keywords: ["position", "right", "offset", "coordinate"]
---

# Right Offset

Set positioned element's right side distance or offset from the parent.

Usage:

- `@position right-*;`

<Alert variant="info">

This page contains only the **property configuration reference**. Complete documentation and usage
examples will be added progressively in future updates.

</Alert>

---

<section>

## Reference

```js title="shilp.config.js"
/** @type {import('shilpcss/types').ShilpConfig} */
const shilpConfig = {
  source: "react",

  properties: {
    position: {
      right: {
        property: "right: <n><v><i>;",
        resolve: "spacing",
        themeKey: "spacing",
        values: {},
      },
    },
  },
};

export default shilpConfig;
```

</section>
