---
title: "Top Offset | @position"
description: "Set positioned element's top side distance or offset from the parent."
keywords: ["position", "top", "offset", "coordinate"]
---

# Top Offset

Set positioned element's top side distance or offset from the parent.

Usage:

- `@position top-*;`

<Alert variant="info">

This page contains only the **property configuration reference**. Complete documentation and usage
examples will be added progressively in future updates.

</Alert>

---

<section>

## Reference

```js title="shilp.config.js"
/** @type {import('shilpcss/types').ShilpConfig} */
const shilpConfig = {
  source: "react",

  properties: {
    position: {
      top: {
        property: "top: <n><v><i>;",
        resolve: "spacing",
        themeKey: "spacing",
        values: {},
      },
    },
  },
};

export default shilpConfig;
```

</section>
